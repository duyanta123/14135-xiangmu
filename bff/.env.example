# ── 服务器配置 ──
NODE_ENV=development
BFF_PORT=4000
BACKEND_URL=http://localhost:8080

# ── JWT（必须设置！使用 openssl rand -base64 32 生成）──
JWT_SECRET=your-secret-key-here
JWT_EXPIRATION=86400000

# ── Redis（Phase 3 启用）──
REDIS_URL=redis://localhost:6379

# ── 日志级别 ──
LOG_LEVEL=info